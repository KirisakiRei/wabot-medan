import { InternalErrorFilter } from './internal-error.filter';

describe('InternalErrorFilter', () => {
  it('should be defined', () => {
    expect(new InternalErrorFilter()).toBeDefined();
  });
});
